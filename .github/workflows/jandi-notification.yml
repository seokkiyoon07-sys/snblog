name: Blog Notification (Jandi + Slack)

on:
  push:
    branches: [main]
    paths:
      - 'src/data/posts.ts'
      - 'src/app/**/page.tsx'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Detect changes
        id: changes
        run: |
          # Check if posts.ts was modified
          if git diff --name-only HEAD~1 HEAD | grep -q "src/data/posts.ts"; then
            echo "posts_modified=true" >> $GITHUB_OUTPUT
          else
            echo "posts_modified=false" >> $GITHUB_OUTPUT
          fi

          # Check if any blog pages were modified
          if git diff --name-only HEAD~1 HEAD | grep -qE "src/app/(originals|startup|columns|problems|reviews|admissions)/.*/page\.tsx$"; then
            echo "pages_modified=true" >> $GITHUB_OUTPUT
          else
            echo "pages_modified=false" >> $GITHUB_OUTPUT
          fi

      - name: Send to Jandi and Slack
        if: steps.changes.outputs.posts_modified == 'true' || steps.changes.outputs.pages_modified == 'true'
        run: |
          # Get commit details
          COMMIT_MSG=$(git log -1 --pretty=%B)
          COMMIT_AUTHOR=$(git log -1 --pretty=%an)
          COMMIT_URL="https://github.com/${{ github.repository }}/commit/${{ github.sha }}"

          # Extract new post info from posts.ts if modified
          if [ "${{ steps.changes.outputs.posts_modified }}" == "true" ]; then
            # Get the latest post from posts.ts (assuming it's the first in allPosts array)
            POST_TITLE=$(grep -A 20 "allPosts.*=.*\[" src/data/posts.ts | grep -m 1 "title:" | sed "s/.*title: *['\"]//; s/['\"].*//")
            POST_EXCERPT=$(grep -A 20 "allPosts.*=.*\[" src/data/posts.ts | grep -m 1 "excerpt:" | sed "s/.*excerpt: *['\"]//; s/['\"].*//")
            POST_THUMBNAIL=$(grep -A 30 "allPosts.*=.*\[" src/data/posts.ts | grep -m 1 "thumbnail:" | sed "s/.*thumbnail: *['\"]//; s/['\"].*//")
            POST_URL=$(grep -A 30 "allPosts.*=.*\[" src/data/posts.ts | grep -m 1 "url:" | sed "s/.*url: *['\"]//; s/['\"].*//")
            
            # Clean up title (remove newlines and extra spaces)
            POST_TITLE=$(echo "$POST_TITLE" | tr '\n' ' ' | sed 's/\\n/ /g' | xargs)
            
            # Set thumbnail URL (use default if not found)
            if [ -z "$POST_THUMBNAIL" ] || [ "$POST_THUMBNAIL" = "undefined" ]; then
              THUMBNAIL_URL="https://images.unsplash.com/photo-1481627834876-b7833e8f5570?w=400&h=200&fit=crop"
            else
              THUMBNAIL_URL="https://blog.snacademy.co.kr$POST_THUMBNAIL"
            fi
            
            # Set post URL
            if [ -n "$POST_URL" ]; then
              FULL_POST_URL="https://blog.snacademy.co.kr$POST_URL"
            else
              FULL_POST_URL="https://blog.snacademy.co.kr"
            fi
          fi

          # Create message based on changes
          if [ "${{ steps.changes.outputs.posts_modified }}" == "true" ] && [ "${{ steps.changes.outputs.pages_modified }}" == "true" ]; then
            BODY="🔄 블로그 콘텐츠가 업데이트되었습니다!"
            DESCRIPTION="새로운 글이 추가되고 데이터가 업데이트되었습니다."
            COLOR="#FF6B6B"
            SLACK_COLOR="danger"
            JANDI_TITLE="📝 SN Academy Blog 업데이트"
          elif [ "${{ steps.changes.outputs.posts_modified }}" == "true" ]; then
            BODY="� 새 포스트가 게시되었어요!"
            DESCRIPTION="$POST_TITLE"
            COLOR="#4ECDC4"
            SLACK_COLOR="good"
            JANDI_TITLE="$POST_TITLE"
          else
            BODY="📝 새로운 블로그 글이 추가되었습니다!"
            DESCRIPTION="새로운 페이지가 생성되었습니다."
            COLOR="#45B7D1"
            SLACK_COLOR="good"
            JANDI_TITLE="📝 SN Academy Blog"
          fi

          # Send to Jandi
          if [ "${{ steps.changes.outputs.posts_modified }}" == "true" ]; then
            # New post notification with thumbnail and title
            curl -X POST "${{ secrets.JANDI_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{
                \"body\": \"$BODY\",
                \"connectColor\": \"$COLOR\",
                \"connectInfo\": [
                  {
                    \"title\": \"$JANDI_TITLE\",
                    \"description\": \"$POST_EXCERPT\",
                    \"imageUrl\": \"$THUMBNAIL_URL\"
                  }
                ],
                \"connectButtons\": [
                  {
                    \"name\": \"포스트 보기\",
                    \"type\": \"link\",
                    \"url\": \"$FULL_POST_URL\"
                  },
                  {
                    \"name\": \"블로그 홈\",
                    \"type\": \"link\",
                    \"url\": \"https://blog.snacademy.co.kr\"
                  }
                ]
              }"
          else
            # General update notification
            curl -X POST "${{ secrets.JANDI_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{
                \"body\": \"$BODY\",
                \"connectColor\": \"$COLOR\",
                \"connectInfo\": [
                  {
                    \"title\": \"$JANDI_TITLE\",
                    \"description\": \"$DESCRIPTION\",
                    \"imageUrl\": \"https://images.unsplash.com/photo-1481627834876-b7833e8f5570?w=400&h=200&fit=crop\"
                  }
                ],
                \"connectButtons\": [
                  {
                    \"name\": \"블로그 보기\",
                    \"type\": \"link\",
                    \"url\": \"https://blog.snacademy.co.kr\"
                  },
                  {
                    \"name\": \"커밋 보기\",
                    \"type\": \"link\",
                    \"url\": \"$COMMIT_URL\"
                  }
                ]
              }"
          fi

          # Send to Slack
          if [ "${{ steps.changes.outputs.posts_modified }}" == "true" ]; then
            # New post notification with thumbnail and title
            curl -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{
                \"text\": \"$BODY\",
                \"attachments\": [
                  {
                    \"color\": \"$SLACK_COLOR\",
                    \"title\": \"$JANDI_TITLE\",
                    \"text\": \"$POST_EXCERPT\",
                    \"thumb_url\": \"$THUMBNAIL_URL\",
                    \"title_link\": \"$FULL_POST_URL\",
                    \"actions\": [
                      {
                        \"type\": \"button\",
                        \"text\": \"포스트 보기\",
                        \"url\": \"$FULL_POST_URL\"
                      },
                      {
                        \"type\": \"button\",
                        \"text\": \"블로그 홈\",
                        \"url\": \"https://blog.snacademy.co.kr\"
                      }
                    ]
                  }
                ]
              }"
          else
            # General update notification
            curl -X POST "${{ secrets.SLACK_WEBHOOK_URL }}" \
              -H "Content-Type: application/json" \
              -d "{
                \"text\": \"$BODY\",
                \"attachments\": [
                  {
                    \"color\": \"$SLACK_COLOR\",
                    \"title\": \"$JANDI_TITLE\",
                    \"text\": \"$DESCRIPTION\",
                    \"thumb_url\": \"https://images.unsplash.com/photo-1481627834876-b7833e8f5570?w=400&h=200&fit=crop\",
                    \"actions\": [
                      {
                        \"type\": \"button\",
                        \"text\": \"블로그 보기\",
                        \"url\": \"https://blog.snacademy.co.kr\"
                      },
                      {
                        \"type\": \"button\",
                        \"text\": \"커밋 보기\",
                        \"url\": \"$COMMIT_URL\"
                      }
                    ]
                  }
                ]
              }"
          fi
